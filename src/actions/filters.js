// SET_TEXT_FILTER
export const setTextFilter = (text = '') => ({
  type: 'SET_TEXT_FILTER',
  text
})

// SORT_BY_TOPIC
export const sortByDate = (topic = '') => ({
  type: 'SORT_BY_TOPIC',
  topic
});

// SORT_BY_TOP

// SORT-BY_NEW